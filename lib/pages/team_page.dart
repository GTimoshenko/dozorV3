import 'package:flutter/material.dart';

class TeamPage extends StatelessWidget {
  const TeamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Здесь будет информация о вашей команде."),
    );
  }
}
